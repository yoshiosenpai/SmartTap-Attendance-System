/**************************************************************************************
 *  secrets.h.example  -  SAFE TO SHARE. Copy to secrets.h and fill in your own values.
 *
 *      cp secrets.h.example secrets.h
 *
 *  secrets.h is the file with your real passwords in it. This one never is.
 **************************************************************************************/
#ifndef SECRETS_H
#define SECRETS_H

// ---------- Wi-Fi ----------
const char* ssid     = "YOUR_WIFI_SSID";
const char* password = "YOUR_WIFI_PASSWORD";

// ---------- MQTT broker ----------
#define MQTT_HOST "192.168.1.100"          // IP of the PC running Mosquitto + Node-RED
#define MQTT_PORT 1883
#define MQTT_USER ""                       // "" if the broker allows anonymous
#define MQTT_PASS ""

// ---------- Optional HTTP fallback ----------
#define HTTP_ENDPOINT "http://192.168.1.100:1880/attendance"

#endif  // SECRETS_H
